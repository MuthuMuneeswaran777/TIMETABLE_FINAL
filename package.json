{
  "name": "timetable-scheduler",
  "version": "1.0.0",
  "description": "Web-based Timetable Scheduling Application with OR-Tools optimization",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": [
    "timetable",
    "scheduling",
    "react",
    "nodejs",
    "mysql",
    "or-tools"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "sqlite3": "^5.1.7"
  }
}
